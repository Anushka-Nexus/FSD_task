TRANSITION property

transition :-it is the property of css which is used for showing the transformation of the element.

transition : property_name , duration(transition ka time) ,delay(kitna delay krvayge uska timing) , timing-function(linear,ease,ease-in,ease-out,ease-in-out,steps,custom) ;


ANIMATION property

syntax :-

 @keyframe animation_name{
    0{
      //timing wise ANIMATION kr skte h  
    }
    from{
        starting phase
    }
    to{
        ending phase
    }
 }


 3d TRANSFORMATION
 perspective : units;
 transform-style : preserve-3d;

  Mis

  filter : property;
   property :-
    grayscale(0,1);
    invert(0,1);


    wekit csslogin page //homework
    uiverse(css element)
    font awesome icons (icons)
    google font(fonts)
